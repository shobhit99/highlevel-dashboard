export enum BulkActionStatus {
    COMPLETED = 'completed',
    IN_PROGRESS = 'in_progress',
    PENDING = 'pending',
    SCHEDULED = 'scheduled'
}

export enum LogStatus {
    queued = 'queued',
    skipped = 'skipped',
    failed = 'failed'
}